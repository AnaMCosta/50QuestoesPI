#include <stdio.h>
#include <stdlib.h>

int main()
{
int x, y, z;
scanf ("%d", &x);
y = x;
z=0;
 while (x!=0) {scanf("%d", &x);
		y= y+x;
		z= z+1;}
		
		
y= y/z;
    
 
printf ("%d", y);

return 0;
}
